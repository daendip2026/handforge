root = true

# C# only: Python formatting is owned by ruff (see .pre-commit-config.yaml).
# end_of_line is intentionally unset to leave existing CRLF alone.
[*.cs]
indent_style = space
indent_size = 4
trim_trailing_whitespace = true
insert_final_newline = true
